query($severity: GqlProbeSeverity, $includes: [String!]) {
    admin {
        jahia {
            healthCheck(severity: $severity, includes: $includes) {
                status {
                    health
                    message
                }
                probes {
                    name
                    severity
                    description
                    status {
                        health
                        message
                    }
                }
            }
        }
    }
}

