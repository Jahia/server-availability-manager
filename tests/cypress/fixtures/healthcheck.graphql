query($severity: GqlProbeSeverity) {
    admin {
        jahia {
            healthCheck(severity: $severity) {
                status {
                    health
                    message
                }
                probes {
                    name
                    severity
                    status {
                        health
                        message
                    }
                }
            }
        }
    }
}

