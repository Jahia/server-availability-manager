query($interval: LoadInterval) {
    admin {
        jahia {
            load {
                requests {
                    count
                    average(interval: $interval)
                }
                sessions {
                    count
                    average(interval: $interval)
                }
            }
        }
    }
}

